import {StyleSheet} from 'react-native'

import {colors, fonts} from '../../constants'

export default StyleSheet.create({
  container: {alignItems: 'center'},
  title: {color: colors.white, fontFamily: fonts.robotoMedium},
})
